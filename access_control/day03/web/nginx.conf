server {
    listen 80;

    # 안내 페이지: 폴더의 파일을 그대로 준다.
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # 계약서 요청: nginx가 받아서 pep 서비스로 넘긴다.
    location /document {
        proxy_pass http://pep:5000;
    }
}
