# AGENTS.md

이 문서는 `security-agent-toolkit` 프로젝트에서 작업하는 모든 AI Agent와 개발자가 반드시 준수해야 하는 엔지니어링 거버넌스 및 협업 규칙을 정의합니다.

---

## 1. 톤앤매너 및 텍스트 작성 규칙

- **언어:** 모든 대화, 주석, 문서, 블로그 포스트는 정갈한 한국어로 작성합니다.
- **이모지(Emoji) 엄격 금지:**
  - 유니코드 이모지, 특수 문자 아이콘(예: 로켓, 전구, 체크, 십자가, 불꽃, 화살표 아이콘 등)을 본문 및 제목에 절대 사용하지 않습니다.
  - 시각적 강조가 필요한 경우 마크다운 표준 문법(볼드, 이탤릭, 인용 블록, 넘버링/불릿 리스트, 코드 블록)만 사용합니다.
- **AI 텍스트 휴머나이징 (`im-not-ai` 기준 준수):**
  - 장황한 도입부, 뻔한 요약문, 기계적 대구("~뿐만 아니라 ~도", "~를 통해 ~할 수 있습니다")를 배제합니다.
  - 번역투 표현(~에 대하여, ~에 있어서, ~되어지다 등)을 피하고 담백하고 명확한 기술 문장으로 서술합니다.
  - 시니어 엔지니어의 테크니컬 라이팅(Technical Writing) 어조를 유지합니다.

---

## 2. 코드 및 디렉토리 무결성 규칙

- **기존 소스코드 수정 제한:**
  - `agent_core/` 내의 파이썬 코드는 비즈니스 로직 수정을 금지합니다.
  - 허용되는 변경은 **파일명 리네이밍**과 **데이터 파일(`logs/`) 이동에 따른 상대 경로 갱신**에 한합니다.
- **데이터 파일 격리:**
  - 각 날짜별 실습에 사용되는 데이터 파일은 `agent_core/dayXX/logs/`에 배치하여 독립적인 재현성을 유지합니다.

---

## 3. Git 커밋 컨벤션

- **기본 형식:** `<type>(<scope>): <subject>`
  - `type`과 `scope`는 소문자 영문으로 작성합니다 (`feat`, `fix`, `refactor`, `docs`, `ci` 등).
  - `subject`는 간결한 한글로 작성합니다.
  - 본문이 필요한 경우 빈 줄 이후 `-` 불릿 목록을 한글로 작성합니다.
- **일일 수업 실습 소스코드 커밋 규칙:**
  - 수업 종료 후 당일 실습 코드를 커밋할 때는 **`feat(<과목명>): Day XX <커리큘럼 허브의 당일 강의 제목>`** 형식을 준수합니다.
  - 예시: `feat(agent_core): Day 06 판단하는 관제 데스크 — LLM·AI 에이전트`
- **최소 작업 단위 커밋:** 기능/파일 변경 단위별로 쪼개어 커밋합니다.
- **보안 검증:** 민감 정보(API 키, 개인정보 등) 및 `.gitignore` 대상 파일이 커밋에 포함되지 않도록 `git status` 및 `git diff`를 항상 사전 점검합니다.

---

## 4. 블로그 포스트 작성 구조 (ADR 포맷)

AI가 블로그 포스트를 생성할 때는 반드시 다음 5가지 핵심 섹션을 포함해야 하며, 주석의 성격에 따라 유동적으로 서브섹션을 구성합니다:

1. **개요 및 학습 개념 요약 (Core Concepts & Insights):** 해당 일차의 핵심 보안/자동화 주제 및 목표 요약.
2. **전체 산출물 파이프라인 구조 (Architecture & Workflow):** 시스템 동작 흐름을 시각화한 Mermaid 다이어그램 1개 이상 포함.
3. **기본 구현의 한계점 (Limitation of Naive Approach):** 강의 예시 수준의 단순 구현이 가진 구조적/보안적 한계 분석.
4. **엔지니어링 의사결정 및 리팩터링 (Engineering Decisions):**
   - 작성자가 주석으로 고민한 질문과 해답을 중심으로 서술.
   - 모듈화, 고차 함수/콜백, 동적 라우트 순회, I/O 최적화 등의 구체적 구현 코드 제시.
   - **심층 리서치/Q&A 주석 반영:** 소스코드 내에 작성된 독립 기술 Q&A(예: Zero Trust, Dual LLM 패턴, Blast Radius 제어, 파라미터 검증 등)는 본 섹션 내 `### 심층 방어 아키텍처` 독립 서브섹션으로 승화하여 실무 보안 설계 관점으로 기술.
5. **검증 및 회고 (Verification & Takeaways):**
   - 동작 검증 결과와 컴퓨터공학적 교훈.
   - **캡스톤 프로젝트 및 실무 확장 로드맵:** 배운 원리를 실무 대규모 시스템이나 캡스톤 프로젝트로 확장할 때 고려할 설계 관점을 서술.

---

## 5. 하네스(Harness) 검증 통과 의무

생성된 모든 블로그 초안은 배포 전 `pipeline/harness.py` 검증기를 통과해야 합니다.
- 이모지 미포함 검증
- 코드 링크 무결성 검증
- 필수 섹션 헤더(#, ##, ###) 포함 여부 검증
- Mermaid 문법 유효성 검증
- 금지된 AI 상투어 검출

---

## 6. 안티그래비티 올인원 에이전트 워크플로우 (평일 17:00)

안티그래비티는 매주 평일 17시 정기 스케줄에 따라 자율적으로 일일 학습 기록 작성 및 검증 파이프라인을 구동합니다.
트리거 시 사용자에게 진행 여부를 묻지 않고 1단계부터 4단계까지 단일 턴에서 전자율로 완주한 뒤, 5단계 컨펌 게이트웨이에서만 대기합니다.
메인 대화 세션의 컨텍스트 과부하(Context Bloat)를 원천 차단하기 위해, 2~4단계 작업은 독립된 메모리 공간을 가진 단발성 서브에이전트(`invoke_subagent`)를 호출하여 격리 수행합니다.

### 1단계. 결정론적 타겟 식별 및 조기 종료 검사 (Mandatory First Step)
1. 미작성 대기 타겟 목록 조회:
   `uv run python pipeline/harness.py --scan-pending`
2. 대기 목록이 0건이면 "모든 실습 및 프로젝트가 이미 블로그 포스트로 작성되었습니다."를 출력하고 즉시 작업을 종료(Stop)합니다.
3. 1건 이상일 경우, 가장 상단의 첫 번째 미작성 대상(예: `course_id='agent_core'`, `day_id='day06'`, `target_slug='c01-agent-core-day06'`, `expected_file='docs/posts/c01-agent-core-day06.md'`)을 이번 세션의 확정 타겟으로 선정합니다.
4. **서브에이전트 표준 프롬프트 주입 및 호출:**
   메인 에이전트는 다음 표준 템플릿으로 서브에이전트(`invoke_subagent`)를 실행합니다:
   ```text
   [타겟]: {target_slug} ({target_type}: {path})
   [필수 선행 로드]: 첫 턴에서 view_file로 다음 스킬 파일들을 반드시 먼저 호출하여 프로토콜을 로드하십시오.
   1) exploring-codebases (C:/Users/User/.gemini/config/skills/exploring-codebases/SKILL.md)
   2) searching-codebases (C:/Users/User/.gemini/config/skills/searching-codebases/SKILL.md)
   3) humanize-korean (C:/Users/User/.gemini/config/skills/humanize-korean/SKILL.md)
   [수행 과업]:
   1. 컨텍스트 인제스트 및 소스코드 교차검증 (AGENTS.md 제6조 2단계)
   2. 5단 ADR 구조 마크다운 초고 작성 및 괄호 영단어 병기 전면 배제 (3단계)
   3. pipeline/harness.py 및 npm --prefix blog run build 자가 교정 완주 (4단계)
   4. 검증 100% 통과 후 최종 초안 전문과 요약을 반환하고 종료.
   ```

### 2단계. 멀티모달 컨텍스트 인제스트 (Zero Manual Effort - Subagent)
1. **과목 실습 타겟(COURSE):**
   - 커리큘럼 허브에서 당일 실습 목표, 강의 제목, 1~6교시 세부 설명, 예제 코드를 자동 인제스트합니다:
     `uv run python pipeline/harness.py --fetch-curriculum <course_id> <day_id>`
   - 대상 디렉토리(`agent_core/dayXX/`)의 실습 소스코드(`*practice.py`, `*llm.py`, `0X_*.py` 등), 주석, `logs/` 데이터를 함께 수집합니다.
2. **자율 프로젝트 타겟(PROJECT):**
   - **`exploring-codebases` & `searching-codebases` 스킬 프로토콜 필수 적용:**
   - 프로젝트 메모와 연결된 원격 레포지토리를 자동 인제스트하고 구조를 분석합니다:
     `uv run python pipeline/harness.py --fetch-project <project_name> <note_file>`
   - `exploring-codebases` 프로토콜에 따라 프로젝트 설정(`docker-compose.yml`, `requirements.txt`), 엔트리포인트(`main.py`), 디렉토리 뼈대 맵 및 핵심 데이터 라이프사이클을 도출합니다.
   - `searching-codebases` 프로토콜에 따라 데코레이터(`@router.get`), 핵심 클래스, 호출 체인을 정밀 슬라이싱하여 실제 소스코드와 메모 내용을 교차검증합니다.
3. **코드 주석 및 리서치 메모의 3단계 유동적 분류 및 배치:**
   - '새로운 개념/원리 메모 주석' -> `## 1. 오늘의 학습 개념 요약`의 본문으로 배치.
   - '설계 고민/문제의식 주석' -> `## 3. 기본 구현의 한계점` 및 `## 4. 엔지니어링 의사결정 및 리팩터링`의 근거로 배치.
   - '독립 기술 Q&A / 실무 리서치 / 캡스톤 연계 주석' -> `## 4번` 섹션 내에 `### 심층 방어 아키텍처` 독립 서브섹션으로 승화(Zero Trust, Dual LLM 패턴, Blast Radius 제어 등)하고, `## 5. 검증 및 회고`의 캡스톤 로드맵에 반영.

### 3단계. 5단 ADR 초안 생성 및 휴머나이징 (im-not-ai 기준 - Subagent)
1. **3단 비교 스토리라인 구성:**
   - 1) 일반적인 기본 베이스라인 코드 스니펫
   - 2) 실제 운영 환경에서의 결함과 보안적/구조적 문제의식
   - 3) 구현 및 최적화 코드 대조 (`*llm.py`가 없으면 주석 기반 단독 리팩터링으로 서술)
2. **`humanize-korean` 스킬 규칙 적용:**
   - 유니코드 이모지 및 특수 아이콘 완전 금지
   - **불필요한 괄호 영단어 병기 완전 배제** (단, API, LLM, JSON, SSOT 등 공인 대문자 약어는 허용)
   - 기계적 대구("~뿐만 아니라 ~도", "~를 통해") 및 AI 상투어 배제
   - 시니어 엔지니어링 테크니컬 라이팅 평어체(`~한다`, `~했다`, `~이다`) 유지
3. **Mermaid 다이어그램 안전 규칙:**
   - 엣지 레이블(`|...|`)에 특수기호(`:`, `?`, `/`, `()`, `&` 등) 배제 및 순수 텍스트/표준 문법 적용.
   - 노드 라벨에 큰따옴표 적용 (`id["라벨 (설명)"]`).
4. **Frontmatter & 5단 필수 본문 구조:**
   - `docs/posts/<target_slug>.md` 경로에 생성.
   - `category`: 과목 실습은 `"AI·보안 자동화"`, 자율 프로젝트는 `"프로젝트 분석 & 회고"`.

### 4단계. 결정론적 하네스 & Astro 블로그 빌드 자가 교정 (Self-Correction - Subagent)
1. 하네스 무결성 검증:
   `uv run python pipeline/harness.py docs/posts/<target_slug>.md`
   (이모지, 필수 헤더, 괄호 영단어 병기, Mermaid 문법 등 하드 에러 0건 필수).
2. Astro 정적 빌드 호환성 검증:
   `npm --prefix blog run build`
3. 하네스 위반이나 Astro 빌드 실패가 발생하면 100% 통과할 때까지 자체 수정 루프를 수행합니다.
4. 모든 검증을 완벽히 통과하면 최종 결과 초안과 요약문을 메인 에이전트에 전달하고 서브에이전트를 종료합니다.

### 5단계. 사용자 컨펌 게이트웨이 (MANDATORY CONFIRMATION GATE - Main Agent)
1. **메인 에이전트는 서브에이전트가 완주한 최종 초고 전문과 핵심 의사결정 요약을 사용자에게 출력하고 명시적으로 대기합니다.**
2. **사용자의 명시적 컨펌(승인)이 있기 전에는 어떠한 경우에도 `git commit` 및 `git push`를 실행해서는 안 됩니다.**
3. 사용자가 수정을 요구하면 대화를 통해 반영 후 하네스/빌드를 재검증하며, 최종 승인 시에만 Git 커밋 및 원격 푸시(또는 PR)를 진행합니다.
