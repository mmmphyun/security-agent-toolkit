# AGENTS.md

이 문서는 `security-agent-toolkit` 프로젝트에서 작업하는 모든 AI Agent와 개발자가 반드시 준수해야 하는 엔지니어링 거버넌스 및 협업 규칙을 정의합니다.

---

## 1. 톤앤매너 및 텍스트 작성 규칙

- **언어:** 모든 대화, 주석, 문서, 블로그 포스트는 정갈한 한국어로 작성합니다.
- **이모지(Emoji) 엄격 금지:**
  - 유니코드 이모지, 특수 문자 아이콘(예: 로켓, 전구, 체크, 십자가, 불꽃, 화살표 아이콘 등)을 본문 및 제목에 절대 사용하지 않습니다.
  - 시각적 강조가 필요한 경우 마크다운 표준 문법(볼드, 이탤릭, 인용 블록, 넘버링/불릿 리스트, 코드 블록)만 사용합니다.
- **AI 텍스트 휴머나이징 (`im-not-ai` 기준 준수):**
  - 장황한 도입부, 뻔한 요약문, 기계적 대구("~뿐만 아니라 ~도", "~를 통해 ~할 수 있습니다")를 배제합니다.
  - 번역투 표현(~에 대하여, ~에 있어서, ~되어지다 등)을 피하고 담백하고 명확한 기술 문장으로 서술합니다.
  - 시니어 엔지니어의 테크니컬 라이팅(Technical Writing) 어조를 유지합니다.

---

## 2. 코드 및 디렉토리 무결성 규칙

- **기존 소스코드 수정 제한:**
  - `agent_core/` 내의 파이썬 코드는 비즈니스 로직 수정을 금지합니다.
  - 허용되는 변경은 **파일명 리네이밍**과 **데이터 파일(`logs/`) 이동에 따른 상대 경로 갱신**에 한합니다.
- **데이터 파일 격리:**
  - 각 날짜별 실습에 사용되는 데이터 파일은 `agent_core/dayXX/logs/`에 배치하여 독립적인 재현성을 유지합니다.

---

## 3. Git 커밋 컨벤션

- **기본 형식:** `<type>(<scope>): <subject>`
  - `type`과 `scope`는 소문자 영문으로 작성합니다 (`feat`, `fix`, `refactor`, `docs`, `ci` 등).
  - `subject`는 간결한 한글로 작성합니다.
  - 본문이 필요한 경우 빈 줄 이후 `-` 불릿 목록을 한글로 작성합니다.
- **일일 수업 실습 소스코드 커밋 규칙:**
  - 수업 종료 후 당일 실습 코드를 커밋할 때는 **`feat(<과목명>): Day XX <커리큘럼 허브의 당일 강의 제목>`** 형식을 준수합니다.
  - 예시: `feat(agent_core): Day 06 판단하는 관제 데스크 — LLM·AI 에이전트`
- **최소 작업 단위 커밋:** 기능/파일 변경 단위별로 쪼개어 커밋합니다.
- **보안 검증:** 민감 정보(API 키, 개인정보 등) 및 `.gitignore` 대상 파일이 커밋에 포함되지 않도록 `git status` 및 `git diff`를 항상 사전 점검합니다.

---

## 4. 블로그 포스트 작성 구조 (ADR 포맷)

AI가 블로그 포스트를 생성할 때는 반드시 다음 4가지 핵심 섹션을 포함해야 합니다:

1. **개요 및 학습 맥락 (Context & Objective):** 해당 일차의 핵심 보안/자동화 주제 및 목표 요약.
2. **기본 구현의 한계점 (Limitation of Naive Approach):** 강의 예시 수준의 단순 구현이 가진 구조적/보안적 한계 분석.
3. **엔지니어링 의사결정 및 리팩터링 (Engineering Decisions):**
   - 작성자가 주석으로 고민한 질문과 해답을 중심으로 서술.
   - 모듈화, 고차 함수/콜백, 동적 라우트 순회, I/O 최적화 등의 구체적 구현 코드 제시.
4. **아키텍처 흐름도 (Mermaid Diagram):** 시스템 동작 흐름을 시각화한 다이어그램 1개 이상 포함.
5. **검증 및 회고 (Verification & Takeaway):** 동작 검증 결과와 컴퓨터공학적 교훈.

---

## 5. 하네스(Harness) 검증 통과 의무

생성된 모든 블로그 초안은 배포 전 `pipeline/harness.py` 검증기를 통과해야 합니다.
- 이모지 미포함 검증
- 코드 링크 무결성 검증
- 필수 섹션 헤더(#, ##, ###) 포함 여부 검증
- Mermaid 문법 유효성 검증
- 금지된 AI 상투어 검출

---

## 6. 안티그래비티 올인원 에이전트 워크플로우 (평일 17:10)

안티그래비티는 매주 평일 17시 10분 정기 스케줄에 따라 자율적으로 일일 학습 기록 작성 및 검증 파이프라인을 구동합니다.

1. **결정론적 타겟 식별 및 컨텍스트 인제스트:**
   - 먼저 `uv run python pipeline/harness.py --scan-pending`을 실행하여 `docs/posts/`에 아직 작성되지 않은 미작성 대기 목록을 조회합니다.
   - 대기 목록이 0건이면 작업을 즉시 종료합니다.
   - **과목 실습 타겟(COURSE):** 커리큘럼 허브에서 당일 실습 목표 및 세부 실습 명세를 자동 인제스트합니다:
     `uv run python pipeline/harness.py --fetch-curriculum <course_id> <day_id>`
     (대상 디렉토리의 실습 소스코드, 주석, logs/ 데이터를 함께 수집).
   - **자율 프로젝트 타겟(PROJECT):** 프로젝트 메모와 연결된 레포지토리를 자동 인제스트합니다:
     `uv run python pipeline/harness.py --fetch-project <project_name> <note_file>`
     (선언형 Frontmatter 기반 2단계 탐색 및 사견 매핑 핵심 소스코드 스니펫 추출).
2. **초안 생성 및 휴머나이징:**
   - **과목 실습:** 초기 프로토타입(`*practice.py`)과 리팩터링 구현체(`*llm.py`) 코드를 대조 분석하여 5단 ADR 구조 블로그 초고(`docs/posts/cXX-*.md`)를 작성합니다 (*llm.py가 없는 경우 주석 기반 구조 개선안으로 단독 서술).
   - **자율 프로젝트:** 인제스트된 프로젝트 프로파일 및 핵심 소스코드와 작성자 사견을 유기적으로 결합하여 5단 ADR 구조 블로그 초고(`docs/posts/proj-*.md`, category: `"프로젝트 분석 & 회고"`)를 작성합니다.
   - `humanize-korean` (`im-not-ai` 기준) 스킬을 적용하여 학생 티를 배제하고 시니어 엔지니어 어조를 유지합니다.
3. **하네스 자가 교정 (Self-correction):**
   - `pipeline/harness.py`를 직접 실행하고, 위반 사항이 발생할 경우 하네스를 통과할 때까지 스스로 수정합니다.
4. **사용자 컨펌 게이트웨이 (MANDATORY):**
   - **에이전트는 하네스를 100% 통과한 초고를 사용자에게 먼저 보고하고 대기해야 합니다.**
   - **사용자의 명시적 컨펌(승인)이 있기 전에는 어떠한 경우에도 `git commit` 및 `git push`를 실행해서는 안 됩니다.**
   - 사용자가 수정을 요구하면 대화를 통해 반영 후 재검증하며, 최종 승인 시에만 Git 커밋 및 원격 푸시를 진행합니다.
